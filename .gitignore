# Python
__pycache__/
*.pyc
.venv/
venv/

# Generated data (large, reproducible via ingestion/pool_postings.py)
data/raw/**/*.jsonl
data/raw/**/*.json

# Generated dashboard (rebuilt by .github/workflows/dashboard.yml, published
# straight to the gh-pages Actions artifact -- not meant to live in git)
dashboard/dist/

# Secrets / credentials (AWS, Snowflake, Databricks tokens)
.env
*.pem
*.key
credentials.json

# Private engineering journal -- kept locally only, not part of the public
# repo (purged from git history 2026-07-23 at the project owner's request)
BUILD_LOG.md

# OS / editor cruft
.DS_Store
.vscode/
.idea/
